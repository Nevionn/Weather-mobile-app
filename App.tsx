import React from 'react';
import {
  SafeAreaView,
  StatusBar,
  StyleSheet,
  Text,
  useColorScheme,
  View,
} from 'react-native';
import MainPage from './src/page/MainPage';

const App = () => {
  return (
    <>
      <MainPage />
    </>
  );
};

const styles = StyleSheet.create({});

export default App;
